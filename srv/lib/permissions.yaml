#
# auth:
#   native
#
# rules:
'*': false

cln1:
  '*': true

items:
  '*': true

cln2:
  insert: "doc.userId === user._id"
  query: "{ userId: user._id}"
  update: "field('userId').notModified() && user._id"
  indices:
    - [('a', 1)]
